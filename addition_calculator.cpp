#include <iostream>
using namespace std;

    int main () {

        // Simple calculator which performs addition only.

        double x,y;

        cout << "Enter first number: ";
        cin >> x;

        cout << "Enter second number: ";
        cin >> y;

        cout << "The sum of the two numbers is: " << x + y;

        return 0;
    }